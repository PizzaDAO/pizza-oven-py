apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rarepizza.fullname" . }}-test-connection"
  labels:
    {{- include "rarepizza.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rarepizza.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
